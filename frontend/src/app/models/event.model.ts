export interface Event {
  archived ?: number;
  end_date ?: string;
  event_ID ?: number;
  event_desc ?: string;
  event_title ?: string;
  first_name ?: string;
  last_name ?: string;
  media ?: any;
  middle_name ?: string;
  roll_no ?: string;
  start_date ?: string;
  progress ?: number;
}
