sudo: required
addons:
  chrome: stable

language: node_js
node_js:
 - 8.11.4
branches:
  only:
    - /.*/
before_script:
 - npm i
 - cd almamatters && npm install -g @angular/cli && npm i && cd ../
script:
 - npm run test
 - cd almamatters
 - ng build --prod
 - ng test
